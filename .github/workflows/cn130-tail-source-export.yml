name: CN130 Tail Payload Apply

on:
  pull_request:
    branches:
      - research/cn130-cross-sectional-ranking-v1
    paths:
      - .github/workflows/cn130-tail-source-export.yml
      - .github/cn130_tail_payload.part*
      - scripts/apply_cn130_tail_payload.py
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
        with:
          ref: research/cn130-tail-factor-discovery-v1
          fetch-depth: 1
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - name: Verify and apply research payload
        run: python scripts/apply_cn130_tail_payload.py
      - name: Commit implementation and evidence
        run: |
          git config user.name 'AlphaEngine Research Agent'
          git config user.email 'research-agent@alphaengine.local'
          git add -A
          git commit -m 'research: execute CN130 tail and factor discovery'
          git push origin HEAD:research/cn130-tail-factor-discovery-v1
