name: QQQI v4.24 XGBoost Adjacent Path Utility

on:
  pull_request:
    branches: [main]
    paths:
      - ".github/workflows/qqqi-v4-24-xgb-adjacent-path-utility.yml"
      - "configs/research_paradigms/qqqi_xgb_adjacent_path_utility_v4_24_research.yaml"
      - "docs/research/qqqi_xgboost_research_synthesis_v4_14_v4_23_2026-08-05.md"
      - "src/research/v4_24_xgb_adjacent_path_data.py"
      - "src/research/v4_24_xgb_adjacent_path_model.py"
      - "src/research/v4_24_xgb_adjacent_path_policy.py"
      - "src/research/v4_24_xgb_adjacent_path_state_machine.py"
      - "scripts/run_qqqi_v4_24_xgb_adjacent_path_utility.py"
      - "tests/test_v4_24_xgb_adjacent_path_utility.py"
  workflow_dispatch:
    inputs:
      end_date:
        description: Optional exclusive market-data end date (YYYY-MM-DD)
        required: false
        type: string

permissions:
  contents: read

jobs:
  evidence:
    runs-on: ubuntu-latest
    timeout-minutes: 240
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install uv and locked dependencies
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"
          uv sync --frozen --extra dev
      - name: Lint v4.24
        run: |
          uv run ruff check \
            src/research/v4_24_xgb_adjacent_path_data.py \
            src/research/v4_24_xgb_adjacent_path_model.py \
            src/research/v4_24_xgb_adjacent_path_policy.py \
            src/research/v4_24_xgb_adjacent_path_state_machine.py \
            scripts/run_qqqi_v4_24_xgb_adjacent_path_utility.py \
            tests/test_v4_24_xgb_adjacent_path_utility.py
      - name: Test v4.24 and inherited research contracts
        run: |
          uv run pytest \
            tests/test_v4_24_xgb_adjacent_path_utility.py \
            tests/test_v4_23_xgb_lambdarank_state_machine.py \
            tests/test_v4_20_credit_duration_action_policy.py \
            tests/test_v4_19_incremental_market_internals.py \
            tests/test_vxn_bridge_allocation_experiment.py \
            -q --strict-markers --tb=short --disable-warnings --maxfail=1
      - name: Run governed v4.24 evidence
        run: |
          for attempt in 1 2 3; do
            if uv run python scripts/run_qqqi_v4_24_xgb_adjacent_path_utility.py \
              --end-date "${{ inputs.end_date }}" \
              --output-dir artifacts/evidence/qqqi_xgb_adjacent_path_utility_v4_24_research; then
              exit 0
            fi
            if [[ "$attempt" == "3" ]]; then
              exit 1
            fi
            rm -rf artifacts/evidence/qqqi_xgb_adjacent_path_utility_v4_24_research
            sleep $((attempt * 30))
          done
      - name: Upload v4.24 evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: qqqi-v4-24-xgb-adjacent-path-utility-${{ github.run_id }}
          path: artifacts/evidence/qqqi_xgb_adjacent_path_utility_v4_24_research
          if-no-files-found: error
          retention-days: 90
