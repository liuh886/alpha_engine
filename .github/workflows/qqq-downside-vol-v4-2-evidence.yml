name: QQQ Downside Volatility v4.2 Evidence

on:
  pull_request:
    branches: [main]
    paths:
      - ".github/workflows/qqq-downside-vol-v4-2-evidence.yml"
      - "configs/research_paradigms/qqq_tqqq_downside_vol_veto_v4_2.yaml"
      - "scripts/run_qqq_tqqq_downside_vol_veto_v4_2.py"
      - "src/research/downside_volatility_veto_experiment.py"
      - "tests/test_downside_volatility_veto_experiment.py"
      - "notebooks/19_qqq_tqqq_downside_vol_veto_v4_2.ipynb"
      - "docs/research/qqq_tqqq_downside_vol_veto_v4_2_experiment.md"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  live-evidence:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install uv and locked dependencies
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"
          uv sync --frozen --extra dev
      - name: Ruff lint downside-volatility paths
        run: >-
          uv run ruff check
          src/research/downside_volatility_veto_experiment.py
          scripts/run_qqq_tqqq_downside_vol_veto_v4_2.py
          tests/test_downside_volatility_veto_experiment.py
      - name: Mypy focused downside-volatility paths
        run: >-
          uv run mypy --follow-imports=skip
          src/research/downside_volatility_veto_experiment.py
          scripts/run_qqq_tqqq_downside_vol_veto_v4_2.py
      - name: Run focused and inherited strategy tests
        run: >-
          uv run pytest
          tests/test_downside_volatility_veto_experiment.py
          tests/test_absolute_breadth_scaling_experiment.py
          tests/test_vxn_exit_persistence_experiment.py
          tests/test_vxn_churn_diagnostics.py
          tests/test_vxn_attack_layer_long_history.py
          tests/test_vxn_leverage_overlay_experiment.py
          tests/test_vix_rotation_experiment.py
          tests/test_strategy_experiment_journal.py
          -q --strict-markers --tb=short --disable-warnings --maxfail=1
      - name: Compile modules and notebook
        run: |
          uv run python -m py_compile \
            src/research/downside_volatility_veto_experiment.py \
            scripts/run_qqq_tqqq_downside_vol_veto_v4_2.py \
            tests/test_downside_volatility_veto_experiment.py
          uv run python - <<'PY'
          import json
          from pathlib import Path
          path = Path("notebooks/19_qqq_tqqq_downside_vol_veto_v4_2.ipynb")
          notebook = json.loads(path.read_text(encoding="utf-8"))
          for cell in notebook.get("cells", []):
              if cell.get("cell_type") == "code":
                  compile("".join(cell.get("source", [])), str(path), "exec")
          PY
      - name: Run frozen downside-volatility veto experiment
        run: >-
          uv run python scripts/run_qqq_tqqq_downside_vol_veto_v4_2.py
          --end-date 2026-08-01
          --output-dir artifacts/evidence/qqq_tqqq_downside_vol_veto_v4_2
          --strategy-run-dir artifacts/strategy_runs
      - name: Upload evidence and strategy run record
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: qqq-downside-vol-v4-2-evidence
          path: |
            artifacts/evidence/qqq_tqqq_downside_vol_veto_v4_2
            artifacts/strategy_runs/qqq_tqqq_downside_vol_veto_v4_2
          if-no-files-found: warn
          retention-days: 14
