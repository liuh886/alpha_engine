// The shipped product is the zero-backend static research studio. Keep the
// default Playwright command aligned with that release contract; live Pages
// acceptance remains isolated in playwright.live.config.ts.
export { default } from './playwright.static.config';
